syntax = "proto3";

option go_package = "rpc/pb";

package pb;

message StringValue {
    string value = 1;
}

message Empty {}

service Serf{
    rpc hello(StringValue) returns (StringValue);
    rpc helloStream(StringValue) returns (stream StringValue);
    rpc query(Empty) returns (stream StringValue);
}